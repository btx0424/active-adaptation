name: Go2Payload
task: Quadruped

defaults:
  # see https://hydra.cc/docs/advanced/overriding_packages/
  - /task/Go2Rough@_here_
  - _self_

terrain: plane
payload: true

observation:
  priv:
    body_mass:      {body_names: [Head.*, "base", "payload"]}
    # body_momentum:    {body_names: [Head.*, "base", "payload"]}

randomization:
  perturb_body_mass_log_uniform:
    payload: [0.01, 5.]